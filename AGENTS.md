# 仓库协作规则

## 适用范围与目标

- 本仓库承载多个独立软件的研究、规划及后续实现。根目录规则适用于全仓库；产品专属规则如有必要，放在其目录的 `AGENTS.md` 中。
- 用户当前明确要求优先于仓库约定。已获授权的文档整理、实现和验证直接推进，不为常规可逆操作反复请求确认。
- 默认用中文沟通和编写规划；产品名称、代码标识与路径用清晰英文。文档使用 UTF-8，优先 LF；PowerShell 读取中文文件显式指定 UTF-8。

## 开始工作

1. 查看 Git 状态与相关规则，保留已有及未提交内容。
2. 阅读根 README、目标产品 README，以及与任务相关的需求和决策记录。
3. 明确本轮处于研究、规划、实验还是实现阶段；用户要求规划时，不擅自推进为完整应用开发。
4. 优先使用 `rg` 查找。只读取必要资料，不重复已完成的研究。

## 目录与文档

- 跨产品研究放 `research/`；产品材料放 `products/<slug>/`。路径使用小写英文和短横线。
- 每个产品首页说明当前阶段、文档入口、下一项可执行工作、尚未验证的限制。
- 每项要求有一个主要定义位置，其他文档链接引用，避免多份参数和状态互相冲突。
- 产品范围调整同步更新需求、路线图和决策记录；不得把建议悄悄改写成用户已确认的决定。
- 记录状态时区分：用户已确认、规划默认、待验证、已验证、延后。
- 不为不存在的应用填写虚构的运行命令、测试结果、版本号或发布状态。

## 研究与决策

- 产品需求优先参考具体用户任务和原始社区讨论；技术能力和平台规则优先参考官方文档、SDK 及可复现实验。
- 对版本、政策、价格等易变信息记录核验日期与来源。推断必须注明；搜索未找到不等于不存在。
- 不把讨论热度当作付费量，不把中文资料较少当作已证实的市场空白。
- 竞品是用途与质量参照。保持独立品牌、界面和实现，不复制其商标、素材或未获许可的代码。
- 不因存在成熟竞品自动排除方向；也不默认新增 AI 就能形成竞争优势。

## 实现与质量

- 先验证会影响项目成立的关键风险，再扩展功能。采用符合平台习惯的交互和可维护的最小架构。
- 有意义的领域逻辑、持久化、错误恢复应有测试；文档调整做链接和一致性检查即可，不为凑覆盖率写测试。
- 性能预算是待测目标，实测结果附环境、方法和限制；不把模拟数据、截图或编译通过当成真机体验验证。
- 修改影响用户流程时同时考虑错误、空状态、键盘、辅助功能、本地化与旧版本兼容。
- 不静默扩大当前版本范围。未来功能记录在产品的延后清单，不先引入相应服务或依赖。

## SonaDeck 当前约束

- 用户已确认：名称 SonaDeck；参考 SoundSource 类用途；支持多套配置，并能在主界面快速切换整套配置；重视 macOS 版本、原生 UI 和动画流畅度。
- 首期功能范围以 [产品需求](products/sonadeck/docs/product-spec.md) 为准；高级功能和 AI 后续再规划。
- 音频数据路径与 UI、配置存储隔离。实时音频回调不执行磁盘、网络、阻塞等待或不可控内存分配。
- 不把系统多项音频设置宣称为真正原子操作；目标配置与实际状态必须分开显示，失败不得显示为成功。
- 不默认采集、保存或上传音频内容。权限按已实现用途申请，不要求用户降低系统安全设置作为默认安装流程。
- 当前工作环境是 Windows；macOS 构建、签名、权限、路由和动画验收必须在 Mac 上执行，不能声称已验证。

## 完成工作时

- 简要说明改动、验证结果、未解决限制和下一步。
- 完成一项工作并完成必要验证后，如有本轮改动，检查差异与工作树，仅提交相关文件到当前分支，并推送至对应远端。提交和推送失败时说明原因及本地状态。
- 未经请求，不自动发布应用、购买服务或向外部用户发送消息。
- 不修改与当前任务无关的产品或用户文件。
